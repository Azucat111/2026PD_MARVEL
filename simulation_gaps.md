# 仿真平台不足审查

以下结论来自 `Env`、`sensor.py`、`MultiAgentWorker`、`NodeManager`、`TestWorker` 及 CodeGraph 调用路径审查。

1. **任务模型缺失**：当前只有探索率/frontier 目标，没有任务实体、优先级、截止期、资源需求、任务切换或失败重试接口。
2. **障碍物过于静态**：`Env.import_ground_truth()` 每个 episode 只加载固定栅格图；没有移动障碍、障碍预测、临时禁飞区或在线地图变化。
3. **动力学过度简化**：机器人在二维平面按 waypoint 线性插值，只有速度和 yaw rate；没有加速度、转弯半径、动力学状态、三维高度、电量和载荷约束。
4. **碰撞模型不完整**：主要解决多个机器人选择同一目标节点，未建模连续时间机器人-机器人碰撞、动态障碍碰撞、机体尺寸、安全距离和紧急制动。
5. **感知过于理想**：射线传感确定性很强，默认完美定位、完美共享地图和完美通信；没有测距/视觉噪声、定位漂移、遮挡误检、通信延迟、丢包和带宽限制。
6. **规模化瓶颈**：节点图固定 padding 到 `NODE_PADDING_SIZE=360`，critic 还接收全体机器人动作；30–60 机时计算、显存和通信量会快速增长，尚未实现稀疏/分簇/区域级协同。
7. **场景配置依赖全局常量**：机器人数量、FoV、传感范围和地图目录主要由 Python 全局参数控制，难以在同一批次混合任务、障碍和异构机器人。
8. **可复现性不足**：地图使用未排序的 `os.listdir()`，随机起点/航向未统一 seed，场景、随机数和模型版本没有完整记录。
9. **评测覆盖不足**：测试入口默认只跑 FoV=120°、6 机、传感范围 10m；没有系统覆盖规模、动态障碍、通信退化、故障恢复和多任务指标。
10. **指标与终止条件不稳健**：完成判断在 worker 中主要依赖 agent 0 的 utility，`Env.check_done()` 使用固定 250 栅格阈值；未统计冲突率、重规划延迟、通信负载、能耗、故障恢复时间和任务切换代价。
11. **工程隔离不足**：训练/测试目录大小写不一致，路径依赖当前工作目录；缺少版本化场景 schema、统一环境 API、配置校验和实验数据格式。
12. **硬件/仿真闭环不足**：论文有 Crazyflie 验证，但开源主体仍是栅格仿真；缺少统一真实状态接口、传感器回放、sim-to-real 参数校准和硬件故障注入。

## 优先级建议

- **P0（先做）**：统一环境 API/配置、动态障碍与连续碰撞、安全屏蔽器、可复现随机种子、规模化稀疏图。
- **P1**：通信延迟/丢包/故障注入、任务状态机、多任务指标和自动评测矩阵。
- **P2**：三维动力学、电量/载荷、传感器回放和 sim-to-real 校准。
