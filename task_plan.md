# MARVEL 揭榜挂帅项目开发计划

## 目标

在现有 MARVEL 受限视场多机器人探索代码基础上，形成面向揭榜挂帅需求的可扩展无人机集群多任务行为决策系统：支持动态/静态障碍、30–60 架规模仿真、自主任务切换、在线重规划与协同行为优化，并交付算法方案、仿真平台、源码、实验数据/分析报告、演示系统和技术报告。

## 范围与基线

- 论文基线：图注意力策略、特权多智能体 critic、前沿/朝向特征融合、信息增益动作剪枝、CTDE + 离散 SAC。
- 代码基线：`MARVEL-main (1)/MARVEL-main`；训练入口 `driver.py`，测试入口 `test_driver.py`，核心模块位于 `utils/`。
- 当前基线限制：二维静态栅格、固定任务（探索）、共享地图/完美通信、默认 4 机器人训练；需扩展而非破坏原始 MARVEL 复现实验。

## 阶段

| 阶段 | 状态 | 交付物 | 验收重点 |
|---|---|---|---|
| 1. 需求与基线固化 | complete | 需求矩阵、代码/论文映射、基准运行说明 | 明确指标、数据结构和复现实验 |
| 2. 可扩展仿真平台 | in_progress | 配置驱动运行时、动态/静态障碍、30–60 机、通信/统一接口 | 4/30/60 机可运行，场景可复现；MARVEL 策略适配尚待完成 |
| 3. 多任务决策与协同算法 | planned | 任务状态机/任务分配、层级策略、CTDE critic、在线重规划 | 任务切换、协同冲突处理、约束满足 |
| 4. 训练工程与性能优化 | planned | curriculum、并行采样、经验回放/评估、模型版本管理 | 训练可复现、吞吐和实时性达标 |
| 5. 验证评测与演示 | planned | 指标脚本、对比/消融、演示系统、数据集 | 效率提升≥15%目标、报告可审计 |
| 6. 交付材料 | planned | 算法设计、源码、实验报告、技术研究报告 | 交付清单完整、部署文档清晰 |

## 下一步任务（按优先级）

1. 建立基线运行检查：依赖、checkpoint、单 episode、100 图测试和资源需求。
2. 定义统一多任务环境 API：任务、状态、约束、事件、奖励、终止条件。
3. 将机器人数量、地图尺寸、障碍动态性、通信模型从全局常量改为场景配置。
4. 设计层级决策：高层任务选择/分配，低层 MARVEL 探索策略；保留 CTDE 训练与分散执行。
5. 扩展 critic 输入以处理任务上下文、邻机状态、通信延迟和故障掩码。
6. 为 30–60 机设计稀疏图、局部邻域、分区/簇级协同，避免 `O(N^2)` 全连接。
7. 加入动态障碍预测、在线重规划、安全屏蔽器和不可行动作回退。
8. 建立指标：任务完成率、总/最大航程、makespan、重规划延迟、冲突率、覆盖率、通信负载、故障恢复时间。
9. 完成基线/消融/压力测试并生成可审计结果。

## 风险与待决策

- 论文代码是二维探索系统，揭榜需求中的多任务、动态障碍和 30–60 机需要架构扩展，不能仅调参解决。
- 当前训练 `TRAIN_ALGO=3` 使用特权 ground-truth critic；部署必须确保策略输入不泄露全局真值。
- 训练参数和测试参数存在目录大小写差异（`marvel`/`MARVEL`），需统一配置管理。
- `marvel.yml` 含原作者 Linux Conda 前缀，Windows/国产算力环境需重新验证。
- 需要在项目早期确认最终任务集合、无人机动力学/通信约束、动态障碍数据来源和“效率提升 15%”的比较基线。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---|---|
| PowerShell 禁止运行 `codegraph.ps1` | 直接执行 `codegraph explore` | 改用 `cmd.exe /c codegraph explore`，成功获得符号与调用关系 |
| 根目录未找到旧计划文件 | `Get-ChildItem` 与 `Get-Content` 结果不一致 | 以当前工作区实际状态为准，在根目录重新创建计划文件 |

## 2026-09-10 落地记录

- 阶段 2 已进入 `in_progress`：新增配置驱动 headless 仿真骨架，未修改原 MARVEL 训练和探索代码。
- 新增 `configs/`、`scripts/`、`platform_tests/`，以及动力学、传感器、通信、障碍物、任务、运行时和评测模块。
- 提供 4 机 `baseline_exploration.yaml` 与 30 机 `urban_rescue_simple.yaml`。
- 已通过 3 个平台测试，完成 30 机 300 步运行并生成配置快照、运行信息、事件日志和评测 JSON。
- 下一步：接入 MARVEL policy adapter、安全动作屏蔽、真实地图加载和 30→60 机压力测试。
- 本轮尚未实现：MARVEL Policy Adapter、原始 `sensor_work_heading` 射线兼容、真实地图文件加载、NoisySensor、SAFETY 任务、角加速度约束和 60 机完整 300 步基准归档。
- 
## 2026-09-19 状态更新

- 阶段 2 仍为 `in_progress`，但“真实 MARVEL checkpoint 推理链”已从待完成推进为已打通。
- 已完成：原始环境 checkpoint 验证、节点起点吸附、360 节点局部图裁剪、30 机策略推理运行。
- 下一阶段任务：验证 30/60 机长时策略运行；修复通信连通率为 0 的场景初始化/通信参数问题；接入动态障碍安全约束、NoisySensor、真实地图加载；再进入多任务调度与评测 baseline。
- 
## 2026-09-20 本轮状态

- 阶段 2：通信初始化、Relay 持续任务、动态障碍事件和多规模评测已完成第一版。
- 阶段 3：高层任务调度器已接入，但尚需根据 60 机结果继续降低碰撞并提升探索率。
- 阶段 5：已获得 baseline 与 MARVEL+调度层的 60 机 300 步首轮对比，暂不宣称达到 15% 综合效率提升。
